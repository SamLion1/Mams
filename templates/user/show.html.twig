{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="vehicle-table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
           {# <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>#}
            {#<tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>#}
            <tr>
                <th>Nom</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ user.firstname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}"><button class="btnAppPerso">Retour à la liste</button></a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}"><button class="btnAppPerso">Modifier</button></a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
